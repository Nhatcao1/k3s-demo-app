[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "he-k8s-client"
version = "0.1.0"
description = "Dependency-free Python client for the trusted HE K8s gateway"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["he_client"]
